#!/usr/bin/env bash

#PBS -l nodes=1:ppn=8:gpus=1
#PBS -l walltime=50:00:00

#PBS -N unet_tensor
#PBS -l vmem=30gb
#PBS -m abe

cd $PBS_O_WORKDIR

export PATH="${VSC_DATA}/miniconda3/bin:${PATH}"
source activate airenv

python train_air.py --type ${TYPE} --comp ${COMP} --fold ${FOLD}
